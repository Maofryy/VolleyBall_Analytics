<?php

namespace App\Controllers;

class Team extends BaseController
{
    /**
     * Probablement la liste des équipes
     */
    public function index()
    {
        
    }

    /**
     * Return infos of teams
     * @param array
     * @return json
     */
    public function getTeams()
    {       
        /*$something = $this->request;
        var_dump($something);die;
        return false;
        
        return 'test';
        $datas = array();

        $playerModel = new \App\Models\Player();
        foreach($teams_id as $key => $team)
        {
            $datas['team' . $key + 1] = $playerModel->getPlayersFromTeam($team);
        }*/
    }
}
